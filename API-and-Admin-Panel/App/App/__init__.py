"""
Package marker for the App application.
Adding this file makes the inner App folder a Python package so the development
runner can import submodules (safe, no runtime logic changes).
"""

# Intentionally empty - package marker
